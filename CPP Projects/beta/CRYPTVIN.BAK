#include<fstream.h>
#include<conio.h>
#include<graphics.h>
//#include<decrypt.cpp>
int encrypt();
int decrypt();
void grafix();
void main()
{int gm,gd=DETECT,vin = 6;
 initgraph(&gd,&gm,"c:\\tc3\\out\\egavga.bgi");
 clrscr();
 grafix();
 int op=3;
 //while(op!=1 || op !=2)
  {
    cout<<"\nEnter 1 for encryption or 2 for decryption : - ";
    cin>>op;
  }
 if (op==1)
  op = encrypt();
 else
  op = decrypt();
}